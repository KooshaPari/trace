---
title: "Docker Setup"
description: "Run services with Docker."
---

## Steps
- Build images or pull from registry.
- Use docker compose profiles for api, worker, and docs.

```bash
docker compose -f docker-compose.dev.yml up api worker docs
```

## Tips
- Mount source for hot reload in dev; use readonly in prod.
- Set resource limits to avoid starving the host.
