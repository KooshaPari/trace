---
title: "Go SDK Workflows"
description: "Trigger workflows via Go."
---

## Go SDK — Workflows
- Recommended for applications already using Go.
- Handles auth, retries, and serialization for you.

## Example
```go
run, _ := client.Workflows.Trigger(ctx, tracertm.TriggerInput{Workflow: "nightly-sync", ProjectID: "proj_123", Payload: map[string]any{"env":"prod"}})
status, _ := client.Workflows.Status(ctx, run.ID)
```

## Tips
- Initialize the client once per process and reuse it.
- Set timeouts and retries explicitly in production.
- Capture request IDs from responses for support cases.
