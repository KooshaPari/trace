---
title: "Go SDK Artifacts"
description: "Work with artifacts in Go."
---

## Go SDK — Artifacts
- Recommended for applications already using Go.
- Handles auth, retries, and serialization for you.

## Example
```go
items, _ := client.Artifacts.List(ctx, tracertm.ListOpts{ProjectID: "proj_123"})
created, _ := client.Artifacts.Create(ctx, tracertm.ArtifactCreate{ProjectID: "proj_123", Name: "Checkout Service", Type: "code"})
```

## Tips
- Initialize the client once per process and reuse it.
- Set timeouts and retries explicitly in production.
- Capture request IDs from responses for support cases.
