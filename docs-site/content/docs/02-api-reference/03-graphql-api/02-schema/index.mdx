---
title: "Schema Reference"
description: "Documentation for Schema Reference"
---

# Schema Reference


## Overview

Complete API reference for Schema Reference.

## Authentication

All API requests require authentication:

```typescript
const headers = {
  'Authorization': 'Bearer YOUR_JWT_TOKEN',
  'Content-Type': 'application/json'
}
```

## Endpoints

### List All Schema Reference

```http
GET /api/v1/schema-reference
```

**Response:**
```json
{
  "data": [],
  "pagination": {
    "page": 1,
    "per_page": 20,
    "total": 100
  }
}
```

### Get Schema Reference by ID

```http
GET /api/v1/schema-reference/:id
```

### Create Schema Reference

```http
POST /api/v1/schema-reference
```

**Request Body:**
```json
{
  "name": "Example Schema Reference",
  "description": "Description here"
}
```

### Update Schema Reference

```http
PUT /api/v1/schema-reference/:id
```

### Delete Schema Reference

```http
DELETE /api/v1/schema-reference/:id
```

## Query Parameters

- `page` - Page number (default: 1)
- `per_page` - Items per page (default: 20)
- `sort` - Sort field
- `order` - Sort order (asc/desc)
- `filter` - Filter criteria

## Error Responses

### 400 Bad Request
Invalid request parameters

### 401 Unauthorized
Missing or invalid authentication token

### 404 Not Found
Resource not found

### 500 Internal Server Error
Server error

## SDK Examples

### TypeScript
```typescript
import { TraceRTMClient } from '@tracertm/sdk'

const client = new TraceRTMClient({ apiKey: 'YOUR_KEY' })
const items = await client.schema-reference.list()
```

### Python
```python
from tracertm import Client

client = Client(api_key='YOUR_KEY')
items = client.schema-reference.list()
```

