---
title: "Event Types"
description: "Documentation for Event Types"
---

# Event Types


## Overview

Complete API reference for Event Types.

## Authentication

All API requests require authentication:

```typescript
const headers = {
  'Authorization': 'Bearer YOUR_JWT_TOKEN',
  'Content-Type': 'application/json'
}
```

## Endpoints

### List All Event Types

```http
GET /api/v1/event-types
```

**Response:**
```json
{
  "data": [],
  "pagination": {
    "page": 1,
    "per_page": 20,
    "total": 100
  }
}
```

### Get Event Types by ID

```http
GET /api/v1/event-types/:id
```

### Create Event Types

```http
POST /api/v1/event-types
```

**Request Body:**
```json
{
  "name": "Example Event Types",
  "description": "Description here"
}
```

### Update Event Types

```http
PUT /api/v1/event-types/:id
```

### Delete Event Types

```http
DELETE /api/v1/event-types/:id
```

## Query Parameters

- `page` - Page number (default: 1)
- `per_page` - Items per page (default: 20)
- `sort` - Sort field
- `order` - Sort order (asc/desc)
- `filter` - Filter criteria

## Error Responses

### 400 Bad Request
Invalid request parameters

### 401 Unauthorized
Missing or invalid authentication token

### 404 Not Found
Resource not found

### 500 Internal Server Error
Server error

## SDK Examples

### TypeScript
```typescript
import { TraceRTMClient } from '@tracertm/sdk'

const client = new TraceRTMClient({ apiKey: 'YOUR_KEY' })
const items = await client.event-types.list()
```

### Python
```python
from tracertm import Client

client = Client(api_key='YOUR_KEY')
items = client.event-types.list()
```

