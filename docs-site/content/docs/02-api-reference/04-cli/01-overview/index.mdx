---
title: "CLI Overview"
description: "Documentation for CLI Overview"
---

# CLI Overview


## Overview

Complete API reference for CLI Overview.

## Authentication

All API requests require authentication:

```typescript
const headers = {
  'Authorization': 'Bearer YOUR_JWT_TOKEN',
  'Content-Type': 'application/json'
}
```

## Endpoints

### List All CLI Overview

```http
GET /api/v1/cli-overview
```

**Response:**
```json
{
  "data": [],
  "pagination": {
    "page": 1,
    "per_page": 20,
    "total": 100
  }
}
```

### Get CLI Overview by ID

```http
GET /api/v1/cli-overview/:id
```

### Create CLI Overview

```http
POST /api/v1/cli-overview
```

**Request Body:**
```json
{
  "name": "Example CLI Overview",
  "description": "Description here"
}
```

### Update CLI Overview

```http
PUT /api/v1/cli-overview/:id
```

### Delete CLI Overview

```http
DELETE /api/v1/cli-overview/:id
```

## Query Parameters

- `page` - Page number (default: 1)
- `per_page` - Items per page (default: 20)
- `sort` - Sort field
- `order` - Sort order (asc/desc)
- `filter` - Filter criteria

## Error Responses

### 400 Bad Request
Invalid request parameters

### 401 Unauthorized
Missing or invalid authentication token

### 404 Not Found
Resource not found

### 500 Internal Server Error
Server error

## SDK Examples

### TypeScript
```typescript
import { TraceRTMClient } from '@tracertm/sdk'

const client = new TraceRTMClient({ apiKey: 'YOUR_KEY' })
const items = await client.cli-overview.list()
```

### Python
```python
from tracertm import Client

client = Client(api_key='YOUR_KEY')
items = client.cli-overview.list()
```

