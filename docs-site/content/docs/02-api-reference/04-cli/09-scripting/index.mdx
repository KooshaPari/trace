---
title: "CLI Scripting"
description: "Automate TraceRTM via shell scripts."
---

## What this covers
- How to manage scripting with the Trace CLI
- Expected inputs, flags, and outputs
- Safety checks before executing

## Quick commands
```bash
trace artifacts list --project <id> | jq "{id,name}"
```

```bash
trace workflows trigger --workflow nightly-sync --project <id>
```

## Best practices
- Use `--project` to scope actions and avoid cross-project edits.
- Prefer idempotent operations (`trace ... upsert`) in automation.
- Pipe to jq for verification before destructive changes.
