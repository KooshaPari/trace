---
title: "Rate Limiting"
description: "Documentation for Rate Limiting"
---

# Rate Limiting


## Overview

Complete API reference for Rate Limiting.

## Authentication

All API requests require authentication:

```typescript
const headers = {
  'Authorization': 'Bearer YOUR_JWT_TOKEN',
  'Content-Type': 'application/json'
}
```

## Endpoints

### List All Rate Limiting

```http
GET /api/v1/rate-limiting
```

**Response:**
```json
{
  "data": [],
  "pagination": {
    "page": 1,
    "per_page": 20,
    "total": 100
  }
}
```

### Get Rate Limiting by ID

```http
GET /api/v1/rate-limiting/:id
```

### Create Rate Limiting

```http
POST /api/v1/rate-limiting
```

**Request Body:**
```json
{
  "name": "Example Rate Limiting",
  "description": "Description here"
}
```

### Update Rate Limiting

```http
PUT /api/v1/rate-limiting/:id
```

### Delete Rate Limiting

```http
DELETE /api/v1/rate-limiting/:id
```

## Query Parameters

- `page` - Page number (default: 1)
- `per_page` - Items per page (default: 20)
- `sort` - Sort field
- `order` - Sort order (asc/desc)
- `filter` - Filter criteria

## Error Responses

### 400 Bad Request
Invalid request parameters

### 401 Unauthorized
Missing or invalid authentication token

### 404 Not Found
Resource not found

### 500 Internal Server Error
Server error

## SDK Examples

### TypeScript
```typescript
import { TraceRTMClient } from '@tracertm/sdk'

const client = new TraceRTMClient({ apiKey: 'YOUR_KEY' })
const items = await client.rate-limiting.list()
```

### Python
```python
from tracertm import Client

client = Client(api_key='YOUR_KEY')
items = client.rate-limiting.list()
```

