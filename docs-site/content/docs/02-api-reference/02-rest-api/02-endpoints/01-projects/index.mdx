---
title: "Projects API"
description: "Documentation for Projects API"
---

# Projects API


## Overview

Complete API reference for Projects API.

## Authentication

All API requests require authentication:

```typescript
const headers = {
  'Authorization': 'Bearer YOUR_JWT_TOKEN',
  'Content-Type': 'application/json'
}
```

## Endpoints

### List All Projects API

```http
GET /api/v1/projects-api
```

**Response:**
```json
{
  "data": [],
  "pagination": {
    "page": 1,
    "per_page": 20,
    "total": 100
  }
}
```

### Get Projects API by ID

```http
GET /api/v1/projects-api/:id
```

### Create Projects API

```http
POST /api/v1/projects-api
```

**Request Body:**
```json
{
  "name": "Example Projects API",
  "description": "Description here"
}
```

### Update Projects API

```http
PUT /api/v1/projects-api/:id
```

### Delete Projects API

```http
DELETE /api/v1/projects-api/:id
```

## Query Parameters

- `page` - Page number (default: 1)
- `per_page` - Items per page (default: 20)
- `sort` - Sort field
- `order` - Sort order (asc/desc)
- `filter` - Filter criteria

## Error Responses

### 400 Bad Request
Invalid request parameters

### 401 Unauthorized
Missing or invalid authentication token

### 404 Not Found
Resource not found

### 500 Internal Server Error
Server error

## SDK Examples

### TypeScript
```typescript
import { TraceRTMClient } from '@tracertm/sdk'

const client = new TraceRTMClient({ apiKey: 'YOUR_KEY' })
const items = await client.projects-api.list()
```

### Python
```python
from tracertm import Client

client = Client(api_key='YOUR_KEY')
items = client.projects-api.list()
```

