name: Quality Checks

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  quality:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Install UV
        uses: astral-sh/setup-uv@v2
        with:
          version: "latest"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Cache uv dependencies
        uses: actions/cache@v3
        with:
          path: ~/.cache/uv
          key: ${{ runner.os }}-uv-${{ hashFiles('**/uv.lock') }}
          restore-keys: |
            ${{ runner.os }}-uv-

      - name: Install dependencies
        run: uv sync --all

      - name: Install pytest-xdist for parallelization
        run: uv pip install pytest-xdist

      - name: Ruff format check
        run: |
          uv run ruff format --check .
          echo "✅ Format check passed" >> $GITHUB_STEP_SUMMARY

      - name: Ruff lint (strict complexity enforcement)
        run: |
          uv run ruff check . --output-format=json > /tmp/quality-ruff-results.json || true
          VIOLATION_COUNT=$(jq '[.[] // []] | length' /tmp/quality-ruff-results.json || echo "0")
          echo "## Quality Check - Ruff Violations" >> $GITHUB_STEP_SUMMARY
          echo "Total violations: $VIOLATION_COUNT" >> $GITHUB_STEP_SUMMARY
          echo "Complexity threshold: max-complexity=7 (pyproject.toml)" >> $GITHUB_STEP_SUMMARY
          uv run ruff check .

      - name: Check for unused imports (pycln)
        run: uv run pycln --check src/

      - name: Type check (mypy - strict mode)
        run: |
          uv run mypy src/ --json-report=/tmp/quality-mypy-report || true
          if [ -f /tmp/quality-mypy-report/index.txt ]; then
            MYPY_ERRORS=$(grep -o "Found [0-9]* error" /tmp/quality-mypy-report/index.txt | grep -o "[0-9]*" || echo "0")
            echo "Mypy errors: $MYPY_ERRORS" >> $GITHUB_STEP_SUMMARY
          fi
          uv run mypy src/

      - name: Type check (basedpyright - ultra-strict)
        run: |
          uv run basedpyright src/ --outputjson > /tmp/basedpyright-results.json || true
          PYRIGHT_ERRORS=$(jq '[.generalDiagnostics // []] | length' /tmp/basedpyright-results.json || echo "0")
          echo "Basedpyright errors: $PYRIGHT_ERRORS" >> $GITHUB_STEP_SUMMARY
          uv run basedpyright src/

      - name: Architecture check (tach)
        run: |
          uv run tach check
          echo "✅ Architecture boundaries validated" >> $GITHUB_STEP_SUMMARY

      - name: Security scan (Bandit - high severity)
        run: |
          uv run bandit -r src/ -l high -i high --format json --output /tmp/quality-bandit-results.json || true
          BANDIT_HIGH=$(jq '[.results // []] | length' /tmp/quality-bandit-results.json || echo "0")
          echo "Bandit high-severity issues: $BANDIT_HIGH" >> $GITHUB_STEP_SUMMARY
          uv run bandit -r src/ -l high -i high

      - name: Install Semgrep (binary; project does not depend on semgrep package)
        run: pip install semgrep
      - name: Security scan (Semgrep - community rules)
        run: semgrep --config=p/security-audit src/
      - name: Security scan (Semgrep - project rules)
        run: |
          if [ -f .semgrep.yml ]; then
            semgrep --config=.semgrep.yml src/
          else
            echo "No .semgrep.yml found, skipping project-specific rules"
          fi

      - name: Docstring coverage
        run: |
          uv run interrogate -vv --fail-under=85 src/
          echo "✅ Docstring coverage ≥85%" >> $GITHUB_STEP_SUMMARY

      - name: Generate quality summary
        if: always()
        run: |
          echo "## Quality Checks Summary (Python ${{ matrix.python-version }})" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Enforced Standards" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Ruff: McCabe complexity ≤7" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Ruff: max-args=5, max-branches=12, max-statements=50" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Mypy: strict type checking enabled" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Basedpyright: ultra-strict mode" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Bandit: security scanning (high severity)" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Tach: architecture boundary enforcement" >> $GITHUB_STEP_SUMMARY

      - name: Upload quality report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: quality-report-${{ matrix.python-version }}
          path: |
            .mypy_cache/
            .ruff_cache/
            /tmp/quality-ruff-results.json
            /tmp/quality-mypy-report/
            /tmp/quality-bandit-results.json
            /tmp/basedpyright-results.json
          retention-days: 90

