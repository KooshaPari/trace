package cache

const (
	// ProjectKeyPattern is the pattern for project cache keys.
	ProjectKeyPattern = "project:*"
	// ItemKeyPattern is the pattern for item cache keys.
	ItemKeyPattern = "item:*"
	// LinkKeyPattern is the pattern for link cache keys.
	LinkKeyPattern = "link:*"
	// AgentKeyPattern is the pattern for agent cache keys.
	AgentKeyPattern = "agent:*"
	// SearchKeyPattern is the pattern for search result cache keys.
	SearchKeyPattern = "search:*"
)
