---
title: "API Keys"
description: "Documentation for API Keys"
---

# API Keys


## Overview

Complete API reference for API Keys.

## Authentication

All API requests require authentication:

```typescript
const headers = {
  'Authorization': 'Bearer YOUR_JWT_TOKEN',
  'Content-Type': 'application/json'
}
```

## Endpoints

### List All API Keys

```http
GET /api/v1/api-keys
```

**Response:**
```json
{
  "data": [],
  "pagination": {
    "page": 1,
    "per_page": 20,
    "total": 100
  }
}
```

### Get API Keys by ID

```http
GET /api/v1/api-keys/:id
```

### Create API Keys

```http
POST /api/v1/api-keys
```

**Request Body:**
```json
{
  "name": "Example API Keys",
  "description": "Description here"
}
```

### Update API Keys

```http
PUT /api/v1/api-keys/:id
```

### Delete API Keys

```http
DELETE /api/v1/api-keys/:id
```

## Query Parameters

- `page` - Page number (default: 1)
- `per_page` - Items per page (default: 20)
- `sort` - Sort field
- `order` - Sort order (asc/desc)
- `filter` - Filter criteria

## Error Responses

### 400 Bad Request
Invalid request parameters

### 401 Unauthorized
Missing or invalid authentication token

### 404 Not Found
Resource not found

### 500 Internal Server Error
Server error

## SDK Examples

### TypeScript
```typescript
import { TraceRTMClient } from '@tracertm/sdk'

const client = new TraceRTMClient({ apiKey: 'YOUR_KEY' })
const items = await client.api-keys.list()
```

### Python
```python
from tracertm import Client

client = Client(api_key='YOUR_KEY')
items = client.api-keys.list()
```

